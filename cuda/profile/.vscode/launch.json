{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run CUDA Executable",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/${fileBasenameNoExtension}.exe",
            "cwd": "${workspaceFolder}",
            "console": "externalTerminal",
            "stopAtEntry": false,
            // "args": ["--debug"]
        }
    ]
}
